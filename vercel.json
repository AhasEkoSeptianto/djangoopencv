{
    "builds": [{
        "src": "djangoopencv/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "djangoopencv/wsgi.py"
        }
    ]
}